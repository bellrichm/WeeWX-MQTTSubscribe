name: Stale Issues

on:
  schedule:
  - cron: "0 0 * * *"
  # - cron: "25 * * * *"
  #workflow_dispatch:

jobs:
  stale:
    uses: weewx-extensions/development_tools/.github/workflows/stale.yml@v1.0.0
